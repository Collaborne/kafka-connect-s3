description = "Kafka Connect Source that reads from S3"

dependencies {
	compile project(':common')

	testCompile project(':sink')
	testCompile 'junit:junit:4.12'
	testCompile 'org.mockito:mockito-all:1.9.5'
}
