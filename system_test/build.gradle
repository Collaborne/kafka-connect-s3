description = "Tests against real Kafka Connect"

dependencies {
	implementation project(':sink')
	implementation project(':source')

	testImplementation 'junit:junit:4.12'
	testImplementation 'org.mockito:mockito-all:1.9.5'

	testImplementation 'org.apache.kafka:connect-runtime:2.6.0'
	testImplementation 'org.apache.kafka:connect-json:2.6.0'
	testImplementation 'org.apache.kafka:kafka_2.13:2.6.0'

	testImplementation 'com.netflix.curator:curator-test:1.2.6'

	testImplementation 'com.spotify:docker-client:6.0.0'
}
